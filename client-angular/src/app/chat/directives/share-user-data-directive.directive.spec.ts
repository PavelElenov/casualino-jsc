import { ShareUserDataDirectiveDirective } from './share-user-data-directive.directive';

describe('ShareUserDataDirectiveDirective', () => {
  it('should create an instance', () => {
    const directive = new ShareUserDataDirectiveDirective();
    expect(directive).toBeTruthy();
  });
});
